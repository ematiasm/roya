# Rebuild the compiled stylesheet and close the coverage gap it hides

## Objective

Regenerate `static/tailwind.css` so every utility class the templates already use actually has a rule, prove the change is purely additive, and add the durable guard that stops the committed stylesheet from silently going stale again.

## Problem

`static/tailwind.css` is generated and committed. It is stale: fifteen utility classes used by templates that exist have no rule in the compiled file, so those elements render unstyled today.

A prior record blamed an older Tailwind version. **That was false and is corrected here.** The committed file's header and the installed CLI are both `v4.3.3`. The file is not behind the tool; it is behind the templates.

Two facts make this more than a rebuild:

1. **The rebuild alone would be unproven on all of its own surface.** The visual baseline visits neither `/settings` nor `/setup`, and **all fifteen** newly styled classes live on exactly those two pages: ten on the Settings Taxes tab and the locale profiles (`self-end`, `whitespace-nowrap`, `sm:grid-cols-[100px_1fr_120px_auto]`, `max-w-2xl`, `w-auto`, `rounded-xl`, `justify-end`, `md:grid-cols-2`, `md:grid-cols-[1fr_auto]`, `md:items-end`, `md:mb-0`) and five on the first-run setup wizard (`max-w-2xl`, `min-h-[70vh]`, `mt-8`, `pt-5`, `sm:grid-cols-2`). The baseline stayed green through the rebuild, and that green is worthless: the net covered **0 of 15**. This is the "a net is only as wide as the pages it visits" failure this repository has already been bitten by, and it is why U2 exists.
2. **Nothing prevents recurrence.** There is no test that a class used by a template has a rule in the committed stylesheet. The stylesheet went stale silently, and it will again.

## Why

- A committed generated artifact is a promise: the app must render correctly from a clean checkout with no CLI installed. Fifteen missing rules break that promise on a real screen, not in theory.
- A rebuild that is not re-stale-able is a one-time fix. The guard is the actual deliverable.

## Decisions

- **Rebuild with the committed command.** `scripts/build-css.sh` is the canonical build, not a hand invocation. Same tool, same input, same flags as any prior rebuild.
- **The change must be purely additive.** A regeneration that removes or modifies a single existing rule is a red flag, not a fact of life, and must be investigated before the file is committed. The measurement is the rule-level diff, not the byte count.
- **The guard is a real test with a real RED.** A test that a class used by a template exists in the compiled stylesheet will be written first, observed failing against today's stale file, and only then satisfied by the rebuild. It must parse class attributes, not prose, and must be escape-aware for arbitrary values and variants, or it will report false positives.
- **Close the baseline coverage gap in the same work.** `/settings` and `/settings?tab=taxes` are added to the visual baseline, with the failing run proving the gap first. That is two recorded follow-ups closed at once.
- **Honest accounting of what the baseline does and does not cover.** The three Settings classes are proven by the baseline once the Settings pages are captured; until then they are only proven by the rule's existence in the compiled file. Both facts get recorded, not conflated.
- Technical artifacts, code, comments, tests, and UI copy are English.

## Authorized scope

Repository-local stylesheet, guard test, e2e harness, and feature-document changes. No push, PR creation, or merge without explicit user request. No database reset, no change to the development database, and no change to `assets/tailwind.css` — the input is already correct and must not be edited to make the output look different.

## Route declaration

Two bounded delegated-direct ODD work units, one writer each: the rebuild and its guard, then the baseline coverage extension. The parent owns task closure, work-unit commits, verification, and delivery reporting. No SDD phase is created.

## TDD mode

- Effective mode: strict TDD inherited from project configuration (`openspec/config.yaml`, `strict_tdd: true`).
- Test runner: `cargo test`, plus the Playwright harness for the baseline.
- The guard's RED is observed against the stale stylesheet, before the rebuild.

## Work units

- [x] U1 — Rebuild the stylesheet and add the staleness guard.
  - The guard was written first and observed RED against the stale stylesheet, naming 15 of 240 class tokens with no rule, each attributed to the templates that use it. GREEN after the rebuild: 247 class names defined, 240 tokens checked, 0 missing.
  - Rebuilt with `scripts/build-css.sh`; `assets/tailwind.css` untouched and a second build byte-identical.
  - The rule-level diff is purely additive: thirteen character-level insertion runs, zero deletions and zero replacements, 679 characters inserted, fifteen selectors added and zero removed. One rule body grew by exactly one declaration, the `--container-2xl:42rem` token `max-w-2xl` consumes.
  - Every one of the fifteen added rules is enumerated below and each corresponds to a class a template actually uses; the guard's RED set and the rebuild's added set are equal.
  - The guard's parsing is anchored, never lexical, and escape-aware by parsing the compiled stylesheet and unescaping per the CSS escape rules rather than substring-matching or hand-escaping. It reads class attribute values, the two `@source` paths the entrypoint declares, `className` assignments and `classList` string arguments, skipping Askama control blocks atomically and stripping comments on both sides.
  - Every behavioural claim in the guard is mutation-proven, not asserted: a class removed from the compiled file fails it, a class added to a template fails it, a removed scan path fails it, and a silently-empty extractor fails it on floors and sentinels. Two real defects were found and fixed this way — a single-argument `classList.toggle` dropping its class, and a method merely *starting* with a writer name (`toggleAll`) being read as `toggle`, which both faked a class and swallowed every call after it.
  - Evidence: `cargo test stylesheet` → 10 passed; `cargo test` → 1117 passed; `cargo check --all-targets` → 0 errors with 0 warnings attributed to the new module; `cargo fmt --check` and `git diff --check` clean; `bash scripts/e2e.sh tests/test_visual_baseline.py` → 1 passed, `tests/test_settings.py` → 6 passed, `tests/test_products.py` → 24 passed / 1 pre-existing opt-in skip. Parent spot check repeated `cargo test stylesheet` → 10 passed. **Commit identity: `1f62560` — `fix(ui): rebuild the stale Tailwind stylesheet and guard it`** (recorded 2026-09-26; the U1 line previously promised to record it and did not).

- [x] U2 — Close the baseline coverage gap for the two unbaselined pages.
  - **The plan as first written was wrong and is corrected here.** It covered only `/settings` and `/settings?tab=taxes`, but five of the fifteen classes (`max-w-2xl`, `min-h-[70vh]`, `mt-8`, `pt-5`, `sm:grid-cols-2`) live exclusively on `/setup`, so U2 as originally scoped would have left them uncovered. U2 must cover the first-run setup wizard too, otherwise the net still misses part of the surface this feature just styled.
  - **A second correction, measured during U2 rather than assumed: `/setup` cannot be a baseline capture at all.** The harness completes first-run setup before any test body runs, and the wizard is one-time, so the seeded instance refuses it. Measured at the wire: with the setup form posted, `GET /setup` answers **303** with `Location: /login`, and the body is empty. Measured in the browser through the real `page` fixture: `page.goto("/setup")` ends at **`/`** with status 200, because the harness's session cookie makes `/login` bounce an already-authenticated visitor to the dashboard — two hops, and the wizard is never rendered. All five classes measure absent from the resulting DOM. A naive `page.goto("/setup")` capture would have recorded the **dashboard**, which is the exact failure this unit exists to prevent.
  - **So `/setup` is covered by a computed-style assertion, not a snapshot**, and the task document says plainly what that costs. A capture for the wizard would need a fresh-database-per-capture server lifecycle; the brief rules that out of this unit and the repository has no such machinery. Instead `reopen_first_run_setup_in_database` in `e2e/helpers.py` removes the singleton `business_settings` row from the throwaway database, which returns the running server to the state a brand-new installation is in. Everything downstream is real: the real route, the real template, the real committed stylesheet, the real browser. This is the suite's second deliberate non-HTTP write to the throwaway database, beside `expire_session_in_database`, and the module docstring now says "two" rather than "one". The development database is never involved, and the fixture's own guard still proves it.
  - **A third correction, and the one that matters most: adding the two Settings pages to the baseline was necessary and NOT sufficient.** A page being visited and a class being *asserted* are two different facts, and the second one was not true for thirteen of the fifteen. Counting each rule's declared property against the suite's own `STYLE_PROPERTIES` list: only `rounded-xl` (`border-radius`, recorded as `border-top-left-radius`) and `pt-5` (`padding-top`) declare a recorded property. The other thirteen declare `width`, `max-width`, `min-height`, `grid-template-columns` (four of them), `justify-content`, `align-self`, `align-items`, `white-space` or a vertical margin — and the vertical margins are excluded from the net on purpose. `width` and `height` were excluded by `ui-component-tokens` because they resolve from font metrics. So before U2, the baseline covered 0 of 15; after adding the pages it covered **1 of 15** (`rounded-xl`), and the other fourteen were merely *present* in a capture. The two new computed-style tests close the other fourteen, taking the total to **15 of 15**.
  - **`w-auto` — this bullet previously claimed the class was inert. That claim was FALSE and is corrected here.** It was generalised from the wide viewport, and it is the exact mistake the module's own `NARROW` constant exists to prevent. Measured on the **live CSSOM** (the rule deleted from the parsed stylesheet, so no served file can be stale or cached), one page load per width: at **1440, 900 and 768 the checkbox is 13px with and without the rule — inert**; at **767, 600 and 320 it is 13px with the rule and 595.547px, 428.547px and 148.547px without it — observable.** The cause is the grid the label sits in (`templates/settings.html:105-112`): at `md` and above the label occupies the `auto` track of `md:grid-cols-[1fr_auto]` and is content-sized, so the flex shrink algorithm lands on the intrinsic 13px either way; below `md` the grid collapses to one track, so `.field`'s `width:100%` becomes the flex base size and the checkbox fills the row unless `w-auto` stops it. The grid track list was read at each width and is the direct evidence: two tracks at 1440/900/768 (`851.547px 84.4531px`, `639.547px 84.4531px`, `507.547px 84.4531px`), one track at 767/600/320 (`667px`, `500px`, `220px`).
    - **The earlier measurement was also produced by a method that cannot be trusted.** It rewrote `static/tailwind.css` between page loads inside a single test and read the result, and it reported 429px with the rule *present* at 600px where the CSSOM method reads 13px. The stylesheet is served by `ServeDir`, so the response is cacheable and a rewrite between loads is not observed reliably. The same file-rewrite method applied *before* a whole test run — a fresh server and a fresh browser context, which is how the mutation sweep works — is sound, and that is why the earlier 14-of-14 mutation result stands. **Rule for the next auditor: compare with and without by mutating the live CSSOM, not the file, whenever the two states share a page load.**
    - `("w-auto", "width", "13px", "13px")` is now in `_SETTINGS_BUSINESS_TAB`, with the narrow reading carrying the evidence and the wide reading kept as the control. The original decision to *delete* the assertion rather than keep a vacuous one was right; generalizing "vacuous" from one viewport into "always vacuous" was the error, and the class is now asserted.
    - **Recording `width` here does not contradict the snapshots' refusal of it.** The snapshots exclude `width` because it resolves from the rendered font and the runner's fonts are not this machine's. 13px is not a text measurement — it is the native checkbox's intrinsic size. Verified with a control inside the same label: a text span measured 63.45px → 179.78px → 368.25px → 613.75px as the label's font became monospace 40px, serif 72px and a font that does not exist at 120px, while the checkbox held **13px** in both its computed `width` and its `offsetWidth` at every one of them.
  - **`whitespace-nowrap` renders only on a tax row**, so the Taxes tab capture — which is the resting state, with no tax defined — does not contain its element. The class is asserted by the computed-style test instead, which defines one tax through `POST /api/taxes`. Seeding a tax *inside* the baseline test was rejected on measurement, not taste: the Products drawer's link-tax `<select>` lists every definition, so a seeded tax would have moved the existing `products-drawer` capture. A capture that needs a seed to be meaningful is a capture that endangers the ones already there.
  - Evidence: the failing run, the containment measurement, the mutation runs, the passing runs, and the exact commands are in Progress and evidence below. **Commit identity: pending — no commit yet**, and this line is here to say so rather than to promise a record that may not arrive. The U1 line above had the same promise and sat unfulfilled through a full cycle; the fix was to record the identity where it belongs, in the U1 bullet and in Progress and evidence.

## Acceptance criteria

1. Every utility class used by a scanned source has a rule in the committed `static/tailwind.css`.
2. A guard test fails when a template uses a class the committed stylesheet lacks.
3. The guard is escape-aware for arbitrary values and variants and produces no false positive on the current tree.
4. The rebuild removes no existing rule and modifies no existing declaration.
5. Every added rule is enumerated and each corresponds to a class a template actually uses.
6. ~~`/settings`, `/settings?tab=taxes` and the setup wizard are covered by the visual baseline, so all fifteen newly styled classes are covered rather than merely present.~~ **Corrected 2026-09-26 (U2): this criterion was written on two assumptions that measurement contradicted, and it is split into what was actually achieved and what was not.**
   - **Achieved: all fifteen classes are covered rather than merely present — 15 of 15**, claimed per class in the table under U2 evidence. One (`rounded-xl`) is asserted by a baseline capture, because `border-top-left-radius` is in `STYLE_PROPERTIES`; all fifteen are also asserted by the two computed-style tests, and **every one of the fifteen is proven by removing its rule from `static/tailwind.css` and watching the owning test fail — 15 of 15 caught.** An earlier draft of this paragraph said fourteen, and recorded `w-auto` as covered only by the Rust presence guard because it was believed inert. That belief was wrong: `w-auto` is inert only at and above the `md` breakpoint and observable below it, and it is now asserted at `NARROW`. The per-class figures in this paragraph were wrong and the coverage was 14 of 15; it is 15 of 15.
   - **Not achieved, and stated rather than quietly reinterpreted:** the setup wizard is **not** covered by the visual baseline. It cannot be, from a harness that completes first-run setup before any test runs; the wizard answers 303 → `/login` and a browser lands on the dashboard having rendered nothing. Closing it as a capture needs a fresh-database-per-capture server lifecycle, which this unit was explicitly not to build. **`/setup` therefore has five asserted classes and zero snapshot coverage.**
7. The visual baseline is regenerated only after a failing run proves the failure is the intended correction.
8. The false "older Tailwind" record is corrected everywhere it appears, without rewriting unrelated history.
9. `assets/tailwind.css` is unchanged.
10. Focused tests, the full Rust suite, and the browser checks pass, with all skips recorded.

## Applicable checks

- `cargo test` (the staleness guard runs in it)
- `cargo test stylesheet`
- `cargo check --all-targets`
- `cargo fmt --check`
- `bash scripts/e2e.sh tests/test_visual_baseline.py`
- `bash scripts/e2e.sh tests/test_settings.py`
- `bash scripts/e2e.sh tests/test_products.py`
- `git diff --check`

## Progress and evidence

- Baseline: branch `feat/tax-calculation-settings` at `e7839cd`, working tree clean apart from the unrelated untracked `odd/tasks/pos-counter-sales.md`, which must never be staged by this task.
- Measured before any change: installed CLI is `tailwindcss v4.3.3`; the committed stylesheet's header is `tailwindcss v4.3.3`. Same tool.
- **Corrected 2026-09-26 (U1).** The pre-change probe recorded here was wrong in three ways; all three are corrected below against the real build. What survives: 27,876 → 28,555 bytes, and 44 top-level blocks in both files (verified on `HEAD` and on the rebuilt file).
  - The character-level diff is **thirteen insertion runs, zero deletions and zero replacements** (679 characters inserted) — not twelve. Measured with `difflib.SequenceMatcher(HEAD, rebuilt, autojunk=False)` over the two file contents and counting the non-`equal` opcodes by kind.
  - **Fifteen selectors are added and zero removed.** The old text said "zero selectors added and zero removed", which contradicted the fifteen inserted rules it listed in the same sentence. Measured at selector level: the parsed rule set grows by exactly fifteen, with 0 selectors removed. (The absolute rule count depends on whether the parse counts the top-level `@layer`/`@supports` blocks as rules, so only the delta and the 15/0 split are stated here.)
  - The class is `md:mb-0`, not `mb-0`. No template uses bare `mb-0`; the only occurrence is `md:mb-0` (`templates/settings.html:111`), and the rebuild adds `.md\:mb-0` inside `@media (min-width:48rem)`. `mb-0` is absent from the committed stylesheet too, but nothing asks for it.
- The fifteen added rules, one per class a template actually uses: `justify-end`, `max-w-2xl`, `min-h-[70vh]`, `mt-8`, `pt-5`, `rounded-xl`, `self-end`, `w-auto`, `whitespace-nowrap`, `sm:grid-cols-2`, `sm:grid-cols-[100px_1fr_120px_auto]`, `md:grid-cols-2`, `md:grid-cols-[1fr_auto]`, `md:items-end`, `md:mb-0` — plus the `--container-2xl:42rem` theme token that `max-w-2xl` consumes.
- The fifteen are exactly the guard's RED set: the guard named fifteen missing classes before the rebuild, and the rebuild adds fifteen rules. No rule without a user, no user without a rule.
- `assets/tailwind.css` is unchanged, and a second `scripts/build-css.sh` is byte-identical to the first.
- **The visual baseline stayed green through the rebuild, and that green is worthless.** Every page it visits was already fully styled, so none of the fifteen newly styled classes appears in any capture. Measured, not predicted: its `goto` list contains no `settings` and no `setup`, and all fifteen classes live on exactly those two pages. A green run over an unvisited page cannot be distinguished from a correct one — this is the "a net is only as wide as the pages it visits" lesson, and it is what U2 exists to close.
- Feature document: `odd/tasks/tailwind-stylesheet-rebuild.md`.
- Engram mirror topic: `odd/tailwind-stylesheet-rebuild/tasks`.
- **U1 delivery: `1f62560` — `fix(ui): rebuild the stale Tailwind stylesheet and guard it`.** Recorded 2026-09-26, after this document twice promised to record it and did not. It is the tip of `feat/tax-calculation-settings` and its parent is `e7839cd`.
- **U2 delivery: pending — no commit yet.** The parent owns the work-unit commit; its identity is recorded here when it exists. The four files it spans are `e2e/tests/test_visual_baseline.py`, `e2e/helpers.py`, `e2e/visual-baseline.json` and this document. Rollback boundary: reverting that one commit removes the two Settings captures, the two computed-style tests and the `reopen_first_run_setup_in_database` helper, and leaves U1's stylesheet and guard entirely untouched.

## U2 evidence

- **`/setup` reachability, measured two ways before anything was built.** At the wire, against a throwaway database spawned exactly the way `conftest.py` spawns it: `GET /setup` answers **200 with the 17,901-byte wizard** while the database has no `business_settings` row, and **303 → `/login` with an empty body** once the setup form is posted. In the browser, through the real `page` fixture of the real harness: `page.goto("/setup")` ends at `http://127.0.0.1:<port>/` with status 200, `max-w-2xl`, `min-h-[70vh]`, `mt-8`, `pt-5` and `sm:grid-cols-2` all **absent** from the resulting DOM, and the same request through the context's request API answers 303 with `Location: /login`. **The harness cannot reach `/setup`, and a naive capture would have silently fingerprinted the dashboard.**
- **The failing run was observed before any regeneration.** With `("settings", "/settings")` and `("settings-taxes", "/settings?tab=taxes")` added to `_pages()` and the baseline untouched, `bash scripts/e2e.sh tests/test_visual_baseline.py` fails with `AssertionError: the interface changed` and every reported difference is `settings: element appeared at …` — a new capture with no baseline to contradict. That run is also the proof the new names are genuinely asserted rather than merely captured: the verify path derives its expected names from `_pages()`, so a page added without a capture cannot pass.
- **Containment, proved by running the unmodified net.** With `e2e/tests/test_visual_baseline.py` reverted to `HEAD` and everything else in place, `bash scripts/e2e.sh tests/test_visual_baseline.py` → **1 passed in 34.59s**: all 77 pre-existing captures are byte-stable. Only then was the baseline regenerated, once.
- **After regeneration: 77 capture names before, 83 after, 6 added, 0 removed, and all 77 common names byte-identical** — compared name by name on `sha256(json.dumps(value, sort_keys=True, separators=(",", ":")))`, 0 changed. The six new names are `settings`, `settings:hover`, `settings:hover-skipped` (165 elements, 20 anchors/buttons, 2 hovered-unsuccessfully) and `settings-taxes`, `settings-taxes:hover`, `settings-taxes:hover-skipped` (113 elements, 19, 2). **`git diff --stat` on the baseline is 1 insertion / 1 deletion**: the file is a single line, so containment is the per-name hash comparison above and not the line count.
- **The new captures audited for what they contain, by walking each page with the baseline's own path scheme and matching class names.** `w-auto` 3/3, `rounded-xl` 3/3, `md:grid-cols-[1fr_auto]` 3/3, `md:items-end` 3/3, `md:mb-0` 3/3, `justify-end` 1/1, `md:grid-cols-2` 1/1 on `/settings`; `self-end` 1/1 and `sm:grid-cols-[100px_1fr_120px_auto]` 1/2 (the create-tax form, not the per-row form) on `/settings?tab=taxes`; and `whitespace-nowrap` **0/1**, because its element exists only on a tax row and the capture is the resting state with none defined. **`whitespace-nowrap` is the only class of the fifteen whose element is not in any capture** — an earlier version of this line named `w-auto` instead, on the false inert claim. All 165 elements the walk found on `/settings` are in the capture, which is the cross-check that the audit and the capture agree.
- **Which of the fifteen is now covered by what — the three-way split, measured rather than argued.**

  | class | page | element in a capture | asserted by |
  |---|---|---|---|
  | `rounded-xl` | `/settings` | yes (3) | **the baseline capture alone** — `border-top-left-radius` is in `STYLE_PROPERTIES` |
  | `justify-end` | `/settings` | yes (1) | computed-style test |
  | `md:grid-cols-2` | `/settings` | yes (1) | computed-style test |
  | `md:grid-cols-[1fr_auto]` | `/settings` | yes (3) | computed-style test |
  | `md:items-end` | `/settings` | yes (3) | computed-style test |
  | `md:mb-0` | `/settings` | yes (3) | computed-style test |
  | `self-end` | `/settings?tab=taxes` | yes (1) | computed-style test |
  | `sm:grid-cols-[100px_1fr_120px_auto]` | `/settings?tab=taxes` | yes (1 of 2) | computed-style test |
  | `whitespace-nowrap` | `/settings?tab=taxes` | **no** (row-only) | computed-style test |
  | `max-w-2xl` | `/setup` | no | computed-style test |
  | `min-h-[70vh]` | `/setup` | no | computed-style test |
  | `mt-8` | `/setup` | no | computed-style test |
  | `pt-5` | `/setup` | no | computed-style test |
  | `sm:grid-cols-2` | `/setup` | no | computed-style test |
  | `w-auto` | `/settings` | yes (3) | computed-style test (at `NARROW`; inert at `VIEWPORT`) |

  So: **1 of 15** is covered by a capture, **15 of 15** are covered by a computed-style assertion on a real render, and **15 of 15** are proven by mutation. Before U2 the honest figure was **0 of 15**, and the baseline's green said nothing about any of them. An earlier version of this table recorded `w-auto` as "nothing proves its effect — measured inert"; that row was wrong and is corrected above and in the U2 bullet.
- **Every new assertion is mutation-proven, not asserted.** Each of the fifteen rules was removed from `static/tailwind.css` **one at a time** and the owning test re-run: **15 of 15 caught**, each with a legible message naming the class and the property (`'md:items-end' computes align-items as ['normal'], expected only 'flex-end'`, `'sm:grid-cols-2' laid out 1 track(s) ['622px'], expected 2`, and for `w-auto` `'w-auto' computes width as ['428.547px'], expected only '13px'`). `static/tailwind.css` was restored after every run and its `sha256` verified identical to the pristine `7c60e2cc44abb2fb79424daed215c0b81f0a1fb858fd0129734bf7d3914bdb32` at the end. With **all fifteen removed at once, all three tests in the module fail** — the net that stayed green through the U1 rebuild now cannot. (An earlier run of this sweep caught 14 of 14; the fifteenth, `w-auto`, was not in the table at the time and is now caught too.)
- **`rounded-xl` is asserted twice, on purpose, and the baseline is the one that matters.** Removing `.rounded-xl{border-radius:var(--radius-xl)}` makes `test_visual_baseline` fail with `border-top-left-radius: '12px' -> '0px'` on the `settings` capture, so the snapshot catches this class on its own — but the class also has an entry in `_SETTINGS_BUSINESS_TAB`, because an auditor reading that table is looking for all fifteen and the overlap is cheap. **Note for whoever audits this next, because it is a silent trap: Tailwind emits the `border-radius` SHORTHAND** (`.rounded-xl { border-radius: var(--radius-xl); }`). A probe that filters the CSSOM on `rule.style.getPropertyValue('border-top-left-radius')` finds **zero** rules for this class and concludes it is inert — while `Array.from(rule.style)` lists the four expanded longhands and the *computed* longhand reads 12px. Measured both ways: count via the longhand **0**, via the shorthand **1**. Match the shorthand, and deleting the rule moves `border-top-left-radius` from 12px to 0px at **1440, 900, 600 and 320** — every width measured, not just one.
- **The variant classes are asserted at two widths, not one.** A value that only holds at one width would be a breakpoint assumption dressed up as a class assertion, so each is read at `VIEWPORT` (1440×900) and at `NARROW` (600×900, below both `sm`=40rem and `md`=48rem): `md:items-end` `flex-end`/`normal`, `md:mb-0` `0px`/`8px` (the `mb-2` base showing through), and the three grid classes 2/2, 2/1, 4/1 tracks. `w-auto` is the case that proves why the second width is not optional: it reads `13px` at both, and **only the narrow reading fails when the rule is deleted**. `sm:grid-cols-[100px_1fr_120px_auto]` additionally pins its literal tracks — track 0 is `100px` and track 2 is `120px` — and deliberately does not pin the `fr`/`auto` tracks, whose pixel values follow the content and are the class of value this file already refuses to pin. (`w-auto` is the one place a `width` is recorded, and the table comment there carries the measurement that justifies it: 13px is the native checkbox's intrinsic size, unchanged across monospace 40px, serif 72px and a nonexistent font at 120px while a control text span in the same label went 63.45px → 613.75px.)
- **Commands and results, U2.** `bash scripts/e2e.sh tests/test_visual_baseline.py` → **3 passed** (40.20s; was 1). `bash scripts/e2e.sh tests/test_settings.py` → **6 passed**. `bash scripts/e2e.sh` (full browser suite) → **112 passed, 4 skipped, 0 failed** (209.90s), against 110 passed + the same 4 skips before U2. The four skips are pre-existing opt-in probes, all named: `tests/test_harness.py:66` (`ROYA_E2E_ARTIFACT_PROBE=1`), `tests/test_harness.py:81` (runs only after that deliberate failure), `tests/test_parties.py:513` (`ROYA_E2E_PARTIES_SCREENSHOT_PROBE=1`), `tests/test_products.py:1403` (`ROYA_E2E_PRODUCTS_SCREENSHOT_PROBE=1`). `cargo test stylesheet` → **10 passed**. `cargo test` → **1117 passed**, unchanged, because this unit touches no Rust. `git diff --check` → **clean**. `static/tailwind.css` and `assets/tailwind.css` are byte-identical to their committed state, and `roya.db`, `roya.db-shm` and `roya.db-wal` keep their sizes and mtimes.
- **Files changed, U2:** `e2e/tests/test_visual_baseline.py`, `e2e/helpers.py`, `e2e/visual-baseline.json`, and this document. No Rust source, no template, no stylesheet.

## Known gaps

- **The first-run setup wizard is still not in the visual baseline, and U2 did not fix that.** What it fixed is the class coverage: the five classes `/setup` owns are asserted by computed style on a real render, so a dropped rule fails. What remains uncovered is the page itself — its other ~90 fingerprinted elements, its hover dimension, and every class on it that is not one of the five, none of which any net checks. Reaching it as a capture needs a fresh-database-per-capture server lifecycle, which U2 was explicitly not to build. The honest summary: **`/setup` has five asserted classes and zero snapshot coverage**, and the cost of closing the rest is a second server fixture, not a test.
- **`w-auto` is asserted at one viewport only, and that is a real limitation of the assertion, not of the class** (U2, after independent verification). The rule is inert at 1440, 900 and 768 and observable at 767, 600 and 320, so the assertion in `_SETTINGS_BUSINESS_TAB` is non-vacuous at `NARROW` and would pass with the rule deleted at `VIEWPORT`. The wide reading is kept as a control, not as evidence. The residual risk is narrow and named: a future change that broke `w-auto` **only** at or above `md` would not be caught. The same shape applies to every variant reading in the two tests — a variant's evidence is the *difference* between the two widths, so a break confined to one side of a breakpoint is only caught if that side is the one the reading is taken on.
- **Comparing a stylesheet with and without a rule by rewriting the file between page loads is unsound** (U2, after independent verification). `static/tailwind.css` is served by `tower_http`'s `ServeDir`, so the response is cacheable, and a rewrite between two loads in the same test is not reliably observed: the method reported the checkbox at 429px with the rule *present* at 600px where the truth is 13px. Rewriting the file *before* a test run — a fresh server and a fresh browser context, which is how the mutation sweep works — is sound. Mutate the live CSSOM when the two states share a page load.
- **Thirteen of the fifteen rebuilt classes are invisible to the baseline's property set even now that their pages are visited** (U2). The four `grid-template-columns` rules, plus `justify-end`, `self-end`, `md:items-end`, `whitespace-nowrap`, `w-auto`, `max-w-2xl` and `min-h-[70vh]` — thirteen — are not in `STYLE_PROPERTIES` at all, and the two vertical margins `mt-8` and `pt-5` are excluded on purpose. Adding those properties to the snapshot would be a large, font-metric-sensitive change to a net that deliberately refuses exactly that class of value, so U2 put the assertions in the two tests that already exist instead. The duplication is real and is the price: a class's protection now lives in the snapshot or in a computed-style test, not in one place.
- **Inline scripts in templates are outside the guard's net.** The guard reads HTML as `class="…"` attributes only, so the **27 inline `className` / `classList` call sites in the templates' `<script>` blocks** are unmonitored: `base.html` (3), and `customers.html`, `documents.html`, `products.html`, `purchase.html`, `purchases.html`, `suppliers.html` (4 each). All 27 are inside a `<script>` block, and between them they write **six** distinct class names — `-translate-x-full`, `flex`, `hidden`, `notice`, `notice-error` and `notice-success` — all six of which have rules in the committed stylesheet. The first three come from the `classList` calls and the notice trio from `box.className` in `base.html`. The direction of the blindness is safe: Tailwind scans those script bodies as raw text, so the compiler's coverage there is a **superset** of the guard's, and the guard can only under-report — never false-positive, and never hide a class the compiler successfully compiled. What it costs is that the guard's coverage claim is scoped to class attributes and to the JavaScript scan sources. Covering inline scripts means parsing JavaScript inside HTML — a second parser to keep correct — and is deliberately out of scope for this unit. Recorded in the module documentation of `src/stylesheet_tests.rs` beside the code.
- **`classList.toggle('a', 'b')` would still drop `'b'`.** `toggle` drops its trailing argument when more than one argument is present, to skip the optional `force` flag. That is right for every call in this repository, but two class names with no flag would lose the second one. No such call exists; a fix would key on whether the trailing argument is a string literal rather than on the count.
- **`{{ … }}` inside a class attribute is a hard failure, not a skip.** There are none today. If one is ever added the guard refuses to pass rather than going blind, which is the safe direction but does mean the failure is a test error until someone extends the extractor.
- **A file type with no extractor in a scanned path panics.** Deliberate: loud beats silently uncovered.
- **`width`, `height` and the vertical margins are not in the visual baseline's property set**, by that suite's own documented decision (they resolve from font metrics). A dropped `w-full` would not be caught there. Pre-existing, from `ui-component-tokens`, not introduced here.
