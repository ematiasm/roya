# templates placeholder

Askama templates live at `templates/` in the crate root (standard Askama convention).
This `src/templates/` directory exists for spec compliance (`src/{main.rs, models.rs, db.rs, services/, routes/, templates/}`).
See `templates/*.html`.
